<%= form_for(@dogshare) do |f| %>
  <% if @dogshare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dogshare.errors.count, "error") %> prohibited this dogshare from being saved:</h2>

      <ul>
      <% @dogshare.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dog_id %><br>
    <%= f.number_field :dog_id %>
  </div>
  <div class="field">
    <%= f.label :sitter_id %><br>
    <%= f.number_field :sitter_id %>
  </div>
  <div class="field">
    <%= f.label :available %><br>
    <%= f.check_box :available %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
